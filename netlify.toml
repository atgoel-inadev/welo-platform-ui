[build]
  base    = "."
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — all client-side routes serve index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
